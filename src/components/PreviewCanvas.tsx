import React from 'react';

// This component appears to be obsolete.
// Keeping a placeholder to prevent build errors regarding html2canvas.
const PreviewCanvas: React.FC = () => {
  return null;
};

export default PreviewCanvas;