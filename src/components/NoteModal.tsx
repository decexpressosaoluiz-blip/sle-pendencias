import React from 'react';

// This component appears to be obsolete and replaced by IssueDetail.tsx
// Keeping a placeholder to prevent build errors.
const NoteModal: React.FC = () => {
  return null;
};

export default NoteModal;